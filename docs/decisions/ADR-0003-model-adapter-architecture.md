# ADR-0003：通过模型适配器解耦

状态：Accepted
日期：2026-07-30

## 背景

BiRefNet、BEN2、SAM 2 和 ViTMatte 的接口、许可证、权重与运行要求不同，模型选型也需要由项目评测决定。

## 决策

应用服务只依赖项目定义的分割、交互分割和 Matting 接口。第三方模型通过独立适配器接入，模型权重管理与最终文件导出分别由专用组件负责。

## 影响

初期需要先设计统一数据契约，但可独立测试工作流、替换模型并比较候选，防止具体模型逻辑扩散到 UI 和业务代码。
