# ADR-0002：质量优先并仅使用本地开源/免费方案

状态：Accepted
日期：2026-07-30

## 背景

工具用于个人离线处理，不追求吞吐量，目标是尽可能获得完整、清晰的物品 Alpha。当前明确不考虑商业 API。

## 决策

所有正式处理均在本机完成，仅采用许可证清楚且适用的开源或免费代码与权重。默认配置以质量优先，允许多阶段推理、全分辨率边缘切块和 CPU 回退。

## 影响

处理时间和模型体积可能增加，安装需处理模型权重和硬件差异。任何降低分辨率或跳过质量阶段的行为必须由用户选择或明确记录，不得静默发生。
