# ADR-0001：采用本地 Web 应用

状态：Accepted
日期：2026-07-30

## 背景

个人工具需要拖放、图片预览、缩放、画布点击和涂抹，同时需要与 Python 模型推理配合。当前不要求原生商店分发。

## 决策

采用仅绑定本机地址的后端与浏览器 UI。图片不上传到外部服务。原型可以使用快速 UI 框架，若交互能力不足，再在保持应用服务接口的前提下替换前端。

## 影响

优点是跨平台、便于模型集成和画布交互；代价是需要管理本地进程和浏览器页面。除非浏览器交互成为阻碍或原生安装包成为明确需求，否则不切换到桌面原生框架。
