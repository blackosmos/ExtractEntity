# ExtractEntity AI 开发规范

## 项目目标

ExtractEntity 是一个完全在本地运行、质量优先的个人物品提取工具。它从图片中识别用户希望保留的物品，允许交互修正，并以原始分辨率导出透明 RGBA PNG。

## 开始任务前

1. 阅读根目录 `README.md` 和 `docs/README.md`。
2. 阅读 `docs/plans/current-milestone.md`、`docs/product/requirements.md`、被分配的 `tasks/TASK-*.md` 及任务引用的文档。
3. 检查当前工作区、现有测试和相关实现，不根据聊天记忆猜测现状。
4. 只有状态为 `Ready` 的任务才可以进入实现；需求不清时先完善任务，不擅自扩大范围。

## 核心约束

- 图片和推理默认全部留在本机，不接入商业 API 或外部图片处理服务。
- 仅采用许可证清楚、符合当前用途的开源或免费依赖；代码许可和模型权重许可分别记录。
- 质量优先于速度，默认保留修正 EXIF 方向后的原始像素尺寸。
- 输出必须是真实 RGBA PNG；不得用生成式模型重绘主体像素。
- UI、工作流编排和具体模型实现解耦，模型通过统一适配器接入。
- 功能完成不等于效果达标；代码测试与固定图片集的视觉评测必须分开。

## 变更规则

- 一次只处理一个明确任务，避免无关重构和顺手增加功能。
- 行为变化必须引用需求 ID，并更新对应规格和测试；架构变化必须新增或更新 ADR。
- 不通过降低验收阈值、吞掉异常、删除测试或更新黄金结果来掩盖退化。
- 新增依赖前记录用途、替代方案、版本和许可证。
- 模型权重、生成结果、缓存、临时图片、评测报告和本机配置不得误提交。

## 文件更新与清理

- 每项事实只有一个权威来源，其他文档使用链接，不复制大段内容。
- 优先更新既有权威文档；仅在内容拥有独立生命周期或明确责任时新增文件。
- 临时计划、实验日志和中间产物必须标注清理时间；任务结束前删除或归档。
- 废弃文档不得与新文档并存为两个“现行版本”：标记 `Superseded` 并链接替代文档，随后按治理规则归档或删除。
- 删除代码或功能时同步删除无用测试、配置、文档、依赖和资产；不得保留“可能以后有用”的死文件。
- 任务完成后检查未引用文件、重复说明、过期命令、调试输出、TODO 和本机路径。
- 详细规则见 `docs/development/repository-hygiene.md`。

## 完成标准

任务只有在以下条件全部满足时才可完成：

- 任务验收标准逐条满足；
- 要求的测试、静态检查和评测已经运行并记录；
- 文档与实现一致；
- 没有无关改动、临时文件和未解释依赖；
- 已知限制和后续事项已记录；
- Git 提交和 PR 清楚描述范围、验证结果与风险。
